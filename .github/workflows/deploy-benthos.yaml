# .github/workflows/deploy-benthos.yml
name: Deploy Benthos to Local Kubernetes

on:
  push:
    branches:
      - main
    paths:
      - 'benthos/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Confirm main branch deployment
        run: echo "✅ Push to main branch detected — running Deploy Benthos workflow -- 1"