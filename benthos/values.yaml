# values.yaml
config:
  input:
    generate:
      mapping: root = "hello from benthos updated"
      interval: 5s
      count: 0
  pipeline:
    processors:
      - mapping: root = content().uppercase()
  output:
    stdout:
      codec: lines