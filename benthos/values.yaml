# values.yaml
config:
  input:
    generate:
      mapping: root = "hello from benthos 4"
      interval: 5s
      count: 0
  pipeline:
    processors:
      - mapping: root = content().uppercase()
  output:
    stdout:
      codec: lines